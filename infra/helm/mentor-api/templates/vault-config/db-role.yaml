{{- if .Values.vaultConfig.enabled }}
apiVersion: redhatcop.redhat.io/v1alpha1
kind: DatabaseSecretEngineRole
metadata:
  name: {{ .Values.vso.dbRole }}
  namespace: vault-config-operator
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  authentication:
    path: kubernetes
    role: vco-admin
    serviceAccount:
      name: controller-manager
  connection:
    address: http://vault.vault.svc.cluster.local:8200 # NOSONAR: internal cluster traffic, TLS handled at mesh level
    tLSConfig:
      skipVerify: true
  path: database
  name: {{ .Values.vso.dbRole }}
  dBName: {{ .Values.vaultConfig.db.connectionName }}-{{ .Release.Namespace }}
  creationStatements:
    - >-
      CREATE ROLE "{{ `{{name}}` }}" WITH LOGIN PASSWORD '{{ `{{password}}` }}'
      VALID UNTIL '{{ `{{expiration}}` }}';
      GRANT SELECT, INSERT, UPDATE, DELETE
      ON ALL TABLES IN SCHEMA public TO "{{ `{{name}}` }}";
      GRANT USAGE ON SCHEMA public TO "{{ `{{name}}` }}";
      GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO "{{ `{{name}}` }}";
  revocationStatements:
    - >-
      REVOKE ALL PRIVILEGES ON ALL TABLES IN SCHEMA public FROM "{{ `{{name}}` }}";
      DROP ROLE IF EXISTS "{{ `{{name}}` }}";
  defaultTTL: 1h
  maxTTL: 24h
{{- end }}
