# On-prem overlay: bare-metal Kubernetes

resources:
  requests:
    cpu: "250m"
    memory: "256Mi"
  limits:
    cpu: "1000m"
    memory: "1Gi"

env:
  - name: SERVER_PORT
    value: "8080"
  - name: LOG_LEVEL
    value: "warn"
