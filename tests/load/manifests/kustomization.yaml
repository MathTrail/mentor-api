apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - k6-load-test.yaml

configMapGenerator:
  - name: mentor-api-k6-scripts
    files:
      - ../scripts/main.js
      - ../scripts/hello.js
      - ../scripts/health.js

generatorOptions:
  disableNameSuffixHash: false
